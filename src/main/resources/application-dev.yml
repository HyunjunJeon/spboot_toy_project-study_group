spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/studygroup
    username: studygroup
    password: studygroup

# Real Service: sendbird, mailgrid 등등 기능을 사용하는게 좋음~
  mail:
    host: smtp.gmail.com
    port: 587
    username:
    password:
    properties:
      mail:
        smtp:
          auth: true
          timeout: 5000
          starttls:
            enable: true

  jpa:
      hibernate:
        ddl-auto: update

